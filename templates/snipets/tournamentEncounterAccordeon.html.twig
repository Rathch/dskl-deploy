
    <div class="accordion-item">
        <h3 class="accordion-header" id="headingTwo">
            <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse-{{groupeNr }}" aria-expanded="false" aria-controls="collapse-{{groupeNr }}">
                {{ headline }}
            </button>
        </h3>
        <div id="collapse-{{groupeNr }}" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#accordionExample">
            <div class="accordion-body">
                {% for encounterRound in encounterRound %}
                    {{ include('snipets/tournamentEncounterTable.html.twig', {encounter:encounterRound}) }}
                {% endfor %}
            </div>
        </div>
    </div>
